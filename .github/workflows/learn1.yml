name: GitHub Actions Demo
on: 
 push:
  branches: ["tom"]
jobs:
  Explore-GitHub-Actions:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: get-process
      - shell: pwsh 
